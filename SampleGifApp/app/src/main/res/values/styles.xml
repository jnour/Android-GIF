<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>


    <style name="AppName" parent="@android:style/TextAppearance.Large">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">bold|italic</item>
    </style>

    <style name="ListTitle" parent="@android:style/TextAppearance.Large">
        <item name="android:textColor">@color/adapter_txt_gray</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="ListDesc" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/adapter_txt_gray</item>
        <item name="android:textStyle">italic</item>
    </style>

</resources>
